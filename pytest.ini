[pytest]
python_files = test_*.py
testpaths = tests
filterwarnings =
    ignore::DeprecationWarning
markers =
    run_e2e: E2E test requiring real tool dispatch (deprecated — wird durch unit-tests ersetzt)
    integration: Cross-domain workflow test
